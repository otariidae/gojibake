{
  "name": "gojibake",
  "private": true,
  "packageManager": "bun@1.3.11",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "bun build ./index.html ./single.html --outdir ./dist --target browser",
    "dev": "bun ./index.html",
    "dev:single": "bun ./single.html",
    "test": "bun test",
    "test:coverage": "bun test --coverage --coverage-reporter=text --coverage-reporter=lcov",
    "check:fix": "biome check . --write",
    "check": "biome check . && bun run build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.8",
    "@happy-dom/global-registrator": "^20.8.9",
    "happy-dom": "^20.8.9",
    "typescript": "^5.9.2"
  }
}
